# Agent 3｜第三页 HTML Demo：执行包与复盘

## 本批唯一执行指令：REQ-30 图3＋图2状态

实际查看 D:/路演方案工作指导/微信图片_20260828184148_47_3165.png 及 微信图片_20260828184145_46_3165.png，完整读[功能锁定](WIREFRAME_FUNCTION_LOCK.md)第2、5—10节。逐项完成R30-P3-01—09和R30-R-01—09：所选行动／执行稿／复制下载／八项实验卡／依据／明确执行状态／新反馈材料及文字／保存／稍后；另在同页提供反馈后复盘四块、改判理由、候选B预览及清单、第二轮规则、明确开始或暂不继续、历史与当前项目入口。

本批新增“显示共享再判断”的职责，覆盖下文旧批次仅定点来源回归范围，但不授权本页造诊断或历史引擎。只写 demo/03-action.html、demo/pages/action.css、demo/pages/action.js、QA_AGENT_3.md。严格使用已选路径／稿件，先取用再自愿反馈，执行默认未知；新反馈文件不得用输入MATERIAL_ADD使当前轮失效。C5—C8由统筹交接口，未交付则显示准确不可用和待接线，不固定A→B、不伪造保存／记忆／下一轮。候选预览不覆盖旧稿，未明确开始不得造正式第二轮。先回执两图／ID／范围／依赖，再在原四文件实施并逐项交回证据。

## 最新方向覆盖：REQ-28

撤回NotebookLM专业工作台作为主布局，三页继续隔离、低上手成本、循序推进。第三页聚焦“拿到内容并使用”：预览、复制/下载和清楚内容切换为主，反馈后置；保留有用功能容器与关键风险，不因撤回三栏参考而把成品散铺成长文。完整信息保留可发现浏览入口，关键风险/重要未知/来源/MoneyAI真实状态不隐藏。豆包仅为用户描述的友好方向，未取得本轮截图，不要求像素复刻；路芽、PC1920×1080、原生HTML与单固定标题动效仍有效。公共样式/状态/Git仍由统筹负责。

状态：本页已按REQ-16完成基础代码交接，当前证据与未验项见QA_AGENT_3。先取用、后自愿反馈的最小范围已确认；不等于此前P3-Q1—P3-Q4全部产品问题冻结。

最新REQ-20已重新下发PC结构改版：用户否定第二、三页功能区不明和回复内容长文平铺。第三页需明确成品/步骤选择、内容预览、就近复制/导出、必要风险/使用条件与独立的后置自愿反馈；不扩展为复杂编辑器或任务看板。要求见[视觉简报](DESIGN_BRIEF.md)，保留已有业务和事件边界。

该结构版本已交接，现追加REQ-23：在本页原四文件内给固定`delivery-title`加`data-fold-title`，引用共享`title-motion.js/css`并在业务异步启动前调用`enhanceFoldTitle(heading)`。初始隐藏标题可等待首次显现；动态行动名、成品、保存/失败状态不动，副标题静态，取用不等动画。用户已明确实施，不再等重新批准开工。另按REQ-25回归首页新输入导致的选择/产物失效、来源和导出，不新增语音入口或改先取用后反馈。依赖与顺序见[实施队列](IMPLEMENTATION_QUEUE.md)；共享升级前不把三问或提取标为可用。

## 你的任务

本页基础实现已获明确授权，按round-2临时方向与demo.v1接口集成；最终视觉与完整SHARED_READY另验，不作为回退到停工的门槛。
把第二页显式选定的路径变成可拿走的成品、清单或实验计划；自愿记录实际执行和观察，再带反馈回第二页。
完成本页与限定验收后交接，不自动开发其他页面、模型接口或后续功能。
先完成1920×1080 PC与主流程，在首屏、关键布局和主要交互节点实际查看并展示图像；手机/Figma专门设计后置。本机记录不等于MoneyAI历史写入/读回；真实业务通路由统筹统一接入。Browser可信路径错误未解决前，备用浏览器须获用户明确许可。

## 开工前必须阅读与核验

1. 阅读 [根目录约定](../../../AGENTS.md)、[开发入口](../README.md)、[当前基线](../CURRENT_BRIEF.md) 和 [资料收件区](../inbox/README.md)。
2. 阅读 [Demo 统筹入口](README.md)、[共享契约](SHARED_CONTRACT.md)、[视觉约定](DESIGN_BRIEF.md) 和 [验收矩阵](QA_MATRIX.md)。
3. 阅读 [第三页讨论稿](../../PAGE_THREE_DISCUSSION.md)，区分当前 Demo 提案、历史默认建议与已确认需求。
4. V0.4 仅是参考材料，不直接搬运其“一问一答”、固定样本量、关键词判无效或 localStorage 状态逻辑。
5. 先查 `git status` 和文件归属；不覆盖现有改动。共享接口缺失或不一致时报告缺项，继续不受影响的本页工作，不抢建公共模块。
6. 统筹已通知六共享文件与运行入口可用；继续本页基础集成，不自行创造另一套视觉或状态接口，不把纯逻辑检查登记为浏览器通过。
7. 按[MCP接入约定](MCP_SETUP.md)验证21st与React Bits，检索文案取用、步骤、轻量反馈或保存动效参考；报告实际成功/失败。连接配置由统筹统一处理，不重复安装、不输出密钥，也不为组件私自换栈。

## 文件归属与技术边界

本任务只允许修改以下文件：

- `demo/03-action.html`
- `demo/pages/action.css`
- `demo/pages/action.js`
- `docs/development/demo/QA_AGENT_3.md`

`demo/shared/` 由统筹先实现，本页只读和调用；不得修改公共 CSS、存储、路由、数据或导出模块。
相邻真实页面为 `demo/01-intake.html` 与 `demo/02-decisions.html`；使用共享约定跳转，不创建替代页面或内嵌另一套应用。
采用原生 HTML、CSS、浏览器 ES modules；不引入 React、构建框架、npm 依赖、远程字体或新增 CDN。
所有共享 API、事件名称、字段、版本和存储键以 SHARED_CONTRACT 为准，本文不另定义接口。
缺少共享能力时提交具体契约缺口，不自己用 localStorage、URL 参数或全局变量绕过共享状态。
使用当前环境可用的 Build Web Apps、Product Design 能力并遵循已确认母版；缺插件如实报告，不宣称已调用。

## 本页应当展示什么

- 顶部简要显示本轮问题、商家/商品范围、轮次、选定路径与版本；提供“回去换一条路径”。
- 主体首先展示该行动真正需要的可用内容；一条回复就给一条，不为凑模块额外生成标题、视频和报告。
- 每个成品附使用位置、事实依据摘要和必要风险；用户可直接复制或导出，不先回答反馈问题。
- 操作清单只包含当前路径必要步骤。实验计划复用第二页已给的指标、观察窗口、分母与限制，不在本页重新诊断。
- 样本下限、预计耗时和预算须附已有依据或“演示假设”；未知保持未知，不默认填100次点击、已跑满或已回滚。
- 面向顾客的成品不能混入商家内部约束，例如“老板没时间重拍”“暂时不能降价”。
- 深层来源、详细依据和历史版本按母版约定展开；关键使用风险不能藏在折叠区或下载文件里。
- 不重做第二页路径比较、大型决策树、置信区间计算或经营仪表盘；完整路径报告沿用第二页入口。

## 成品取用与编辑

复制成功必须来自实际剪贴板操作成功；失败保留文本并给手动选取方式，不显示“已复制”。
导出必须生成真实可读取文件，内容、编码、文件名和来源标签按共享契约；不能放假下载链接。
浏览器发起下载最多记录 `download_requested`，不能宣称文件已经落盘；导出不代表选择、采用或执行。
模板、规则拼装或预写参考稿必须持续标明“演示生成/参考稿”；不显示真实 MoneyAI 生成或长期记忆成功。
允许修改文字口吻并保留稿件版本；涉及规格、价格、承诺或行动范围的修改，须按共享失效机制重新核对。
缺少必要商品事实时展示待补信息或核对步骤，不能编造发货、运费、效果、尺寸或售后保证。

## 轻量反馈：随时可选，不拦取用

反馈入口为“记一下实际情况”，默认不展开整套表单；退出、不反馈或回第二页都不算拒绝或失败。
最小输入只有“动作是否实施”与一句实际观察，二者均允许未知；计数、执行日期和观察期只在用户主动展开时出现。
观察标签如“好一些/没变化/变差”必须由用户明确选择；不得仅靠关键词把一句模糊描述转换为成效结论。
不重新询问首轮行业、商品、流水或漏斗；已有来源与限制直接读取，同轮补问额度跨页共享，不重置。
用户可只记录部分实施、阻碍或新情况；保存执行时间未知的记录是合法路径，不为了保存强迫补日期。

## 状态必须分清，不把状态机摆成问卷

| 语义 | 本页约束 |
| --- | --- |
| 实际执行 | `unknown / not_started / partial / done`；默认 unknown，完成是商家自述，不是平台核验 |
| 结果观察 | `unknown / better / unchanged / worse`；独立于执行状态，未观察不填0或unchanged |
| 选择与采用 | 读取第二页显式选择；复制、下载不自动变成采用；无采用陈述时保持未知 |
| 时间与范围 | 分开实际执行时间、反馈时间和保存时间；未提供执行时间就留空，保留实际做了哪些部分 |
| 保存与读取 | 草稿、保存中、成功、失败、读回成功分别记录，不把本地保存说成云端或跨设备同步 |

点击“变差”不能写成“未采用”；部分实施后停止，仍保留已实施部分，不回写为从未执行。
一句“用了”不能自动标记所有片段已发布；范围不明时按自述保存，并保留未确认范围。
查看、选择、复制、下载请求、采用、执行、结果、保存和读回必须保持独立事件及版本关联。

## 保存、读回与下一轮

通过共享模块保存，不自行创建数据库；共享方案使用 IndexedDB 保存 JSON/Blob，页面等待事务完成后的成功确认。
成功后才显示“已保存到本机浏览器”，并注明对应行动/版本与实际保存时间；保存前是草稿。
禁用存储、容量不足或写入失败时保留当前草稿，说明尚未保存；允许重试，不重复生成同一条正式反馈。
刷新后必须真实读回对应记录，才显示“已读取本机记录”；读取失败不能用预载案例伪造历史。
提供显式“带着反馈再分析”入口，通过共享流程建立新 round，再跳回第二页；不能在本页预写“下一轮已改善”。
携带上轮事实、限制、选路、成品版本和真实执行/观察记录；按记录性质区分模拟、自述、已保存与实际读回。
共享创建流程须处理重复点击：一次确认只创建一个新 round，保留历史与事件，不把数字直接改成固定的2。
未保存草稿若要带入下一轮，须由共享契约明确支持并标为未持久化；否则提示先保存，不静默丢弃或伪称读回。
“没变化”只是一条观察，不能自动证明行动失败或强制换路；样本不足、时间不明或口径不同应带回第二页再判断。
出现恶化可以提示原计划已有的暂停/回滚条件，但不能自动记录已暂停、已回滚或已完成新行动。

## 必须处理的异常与版本变化

- 直接打开本页但没有显式选路：展示说明并引导第二页，不替用户默认选A，也不显示虚构成品。
- 输入或选路版本变化：旧成品标为历史只读/待更新，不继续作为当前有效建议；历史执行仍指向旧版本。
- 回第二页换路：保留旧执行和反馈；未保存编辑须提示真实去向，不因导航清空记录或变成拒绝。
- 旧版本若允许导出，仅作为明确标注的历史材料，不能继续提供无提示的“拿去执行”。
- 共享状态缺失、损坏或版本不兼容：使用契约定义的恢复入口，不私自迁移、清空或覆写别页状态。

## 验收：最多8项，结果写入自己的QA文档

| 编号 | 操作与应观察结果 |
| --- | --- |
| A3-01 | 从第二页选路进入：只见对应成品；不填反馈可复制/导出，事件不变成采用或执行；复制失败有真实提示 |
| A3-02 | 清空可用选路后直达：安全引导第二页，无默认A、假历史或占位成品 |
| A3-03 | 记录部分执行＋变差且不填执行时间：两种状态独立，执行时间未知，反馈时间单独保存，不写成未采用 |
| A3-04 | 正常保存并刷新读回；再模拟存储禁用/失败/重试：成功与失败准确，草稿保留，无重复正式记录 |
| A3-05 | 输入变更或换路径：旧成品失效或只读，历史执行不丢失，未保存稿件有明确去向 |
| A3-06 | 显式带反馈再分析并重复点击：仅新建一轮，携带来源与真实记录；没变化不被自动判为失败或已回滚 |
| A3-07 | 打开实际导出的文件：包含正确成品、版本、来源和必要风险；模板有演示标签，只记下载请求、不声称落盘 |
| A3-08 | 按QA_MATRIX做宽窄屏、键盘、焦点、文本可读性及控制台检查；未运行的检查明确标未执行 |

使用统筹提供的合成场景，不在本页硬编码一套新商家和既定历史。不得上传私密原件、密钥或个人流水作测试。
依照共享运行方式验收；仓库静态检查与浏览器行为验收分开，不能用构建通过替代复制、存储或新轮次验证。

## 交接格式与停止点

交接说明：修改文件；依赖的共享契约/视觉版本；实际运行命令；8项验收逐项结果与证据；未执行项；已知风险和阻塞。
在 `QA_AGENT_3.md` 记录可复现步骤、真实页面地址及截图位置；不把模板示例或模拟结果写成商家效果。
需要改共享模块时，只在交接中提出缺口与影响，交给统筹处理；不自行修改公共文件、其他页面或提交Git。
完成后停止，等待统筹集成和用户审阅；本页完成不代表第三页最终产品方案、模型接入或真实经营验证获批。
