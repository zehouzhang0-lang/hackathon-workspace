# 三页共用视觉简报

## 最新功能母版：REQ-30 四张草稿

张泽厚明确要求按四图对应功能实施：[逐项要求与原图身份](WIREFRAME_FUNCTION_LOCK.md)。本机原图已看，尺寸1586×992，非运行截图；主交付仍为1920×1080。图1的三块输入／六组确认、图4的链路／依据／判断反馈／A/B、图3的执行稿／实验卡／反馈、图2的反馈复盘／候选下一轮必须保留位置关系与操作层级。

图2作为P3状态，不扩为第四页；撤回旧语音主入口、手动折叠、九组主展示，与此冲突的下文参考不再是本批要求。路芽名称及现版IP不因图中旧名称改变，原图不上传；不能擅自删功能或改成其他工作台。允许按PC尺寸和中文可读性调整比例，最终色值、细节与运行视觉仍由用户确认。漏斗数学、未知、演示／真实来源、执行和保存语义按功能锁定第7节，不照抄草稿中的不准确断言。

状态：**已有三页基础代码与临时共同样式，尚无最终批准的视觉母版。** 下列要求不是最终色板或视觉验收结论。
最新用户授权按[REQ-16](../CURRENT_BRIEF.md)覆盖此前等待最终母版的开工顺序：先用现有修订作临时方向实现三页基础版本，视觉并行完善；这不把任何候选登记为最终批准，也不要求重新批准已接受的Demo范围。

## 2026-08-28 最新用户反馈

### 三页渐进展示（REQ-28，覆盖工作台主布局建议）

用户明确认为专业工作台的上手成本不适合本产品；撤回DF003中“NotebookLM作功能区结构主参考”的建议。三页继续隔离，每页只承担当前阶段；豆包是用户描述的友好与渐进方向，没有本轮截图依据，不要求像素复刻或新增无限聊天。

- 第一页“说清情况”：语音主入口、材料次入口、手动折叠，核对和纠错随当前步骤出现，不把九组字段做成初始长问卷。
- 第二页“看清并选下一步”：简短分析、可比较路径、关键代价/风险/未知和明确选择操作构成默认页面。完整分析、依据和条件分支用有名字的入口查看并返回；不堆专业控制台，也不退回长文平铺。
- 第三页“拿到内容并使用”：当前成品预览、复制/下载优先，内容切换清楚，可选反馈后置。现有有用容器可保留，不因撤回三栏参考就去掉功能边界。
- 选择前的关键风险、重要未知、来源和MoneyAI真实状态不隐藏；其他依据、假设、方案有可发现的浏览入口，不以难发现的“更多”取代，不造假历史功能。
- 返回与查看其他路径保留输入；查看/选择/采用/执行分开，复制或下载请求不等于本地记录已保存、文件已落盘或已执行。改选后按真实版本显示下游失效。

这是已确认的产品方向，不是已通过1920×1080运行截图验收。路芽、REQ-25最高优先、单固定标题动效和PC先行仍有效；不增加组件依赖、不换栈或开始手机稿。

### 路芽品牌决定（REQ-26）

用户已确定产品名“路芽”，IP使用当前版本，沿用“把下一步看清楚，把每一次选择接起来”的理念；覆盖下文历史段落中的“正式名称待定”。现在进行与三页功能结构相容的品牌统一适配，首页语音仍优先。品牌任务负责规范，统筹统一公共壳和主题；不重画IP、不自动下载字体或更换技术栈。原图是概念图，不把它当透明资产或全站最终视觉验收；素材入私有GitHub许可仍另行确认。

品牌任务已交[路芽全站适配规范](../brand-ip/LUYA_SITE_STYLE.md)及检查记录，统筹已实际读取。公共壳先加入文字“路芽”、三步任务用语及统一页面标题；未换全站色板、未裁切或引用私有IP图。暖白/深青绿/琥珀及字号仍为待实看建议，不能将错误/风险的accent随品牌色一起替换。

减少AI感通过具体中文、明确主次和克制装饰落实，不删掉MoneyAI来源、合成演示标签、失败/未知与保存边界。工作流参考研究见[DF-003](../design-feedback/DF-20260828-003-workflow-references.md)；NotebookLM主布局建议已由REQ-28撤回；其他公开研究仅保留可用的功能分组观察，不代替用户确定的三页渐进方向。

### PC基础页后的重要调整（REQ-20—21）

用户已查看基础页面并指出第二、三页“不专业”：功能布局不明确、信息散落，不允许回复性内容缺少窗口、按钮与明确功能区而像单薄Word文档一样平铺。**当前第二、三页呈现需要结构改版，不是视觉已验收。** 所附`codex-clipboard-57aea48a-48d4-40e2-b913-bde20e94e5aa.png`显示第二页；统筹已实际查看，第三页本次批评来自用户，尚无对应新截图，不假称已目视审查第三页。

- 第二页先呈现路径比较和当前路径工作区域；压缩标题/问题/元信息，让可选路径、选中状态、当前动作与主要按钮在1920×1080首屏清楚。详细上下文与依据可展开，但关键风险和未知不能藏掉。
- 当前查看路径与已经保存的选择分别标注。浏览其他路径、切换详情、下载报告都不改变选择；当前查看路径未选时提供明确选定操作，已选且版本有效时提供前往取用的主操作。状态与操作放在同一区域，保存失败、旧版本和忙碌不能显示成选定成功。
- 行动与投入、结果与风险、证据与决策树采用有明确用途的分区或切换；视觉边界、标题、主次按钮和状态相配合，不把全部内容连续堆成报告，不给每段机械套框。
- 第三页以成品/步骤选择、明确的内容预览区域和就近复制/导出为主工作区；使用条件/必要风险有独立位置。后置可选反馈与取用区分开，不扩展为复杂编辑器或任务看板。
- “减少无关框体”仍有效，但不应解释为“不要功能容器”。是否需要面板、抽屉、标签或折叠，以任务清晰、关键风险可见和中文可读为准，不能为了动效换栈或增加未经批准能力。
- 原第二、三页任务在各自文件内继续PC结构改版，公共主题与业务契约保持一致。过程给出可看的结果，最终视觉仍由用户选择；手机/Figma专门设计后置。

新增用户任务“视觉反馈与动效评审”`01a046ec-d06b-75b3-85fe-e02a9c9c9a34`专门接收与评阅外界参考，独占`docs/development/design-feedback/`与`demo/assets/design/feedback/`，只向统筹提出有证据的调整建议，不改三页/公共样式或直接派活。此任务允许接收图片、录屏、链接和文字意见；来源、适用范围、未批准建议与用户明确要求分别记录。

首轮交接已收到并核对：[反馈入口](../design-feedback/README.md)、[DF-20260828-001](../design-feedback/DF-20260828-001.md)及[原图登记](../../../demo/assets/design/feedback/README.md)。原图1891×955，CSS视口、DPR与缩放未知；画面已有选定提示，不能当成未选初始态，也不能因画面未见主按钮断言整页无按钮。统筹将三项建议纳入本轮结构实施和待执行验收，不把建议或静图登记为用户最终视觉批准。1920×1080实测时应无需滚动即可辨认两条示例路径、当前查看/已有选择、主要操作及关键限制，不靠缩小正文或隐藏风险达成。

### 固定标题动效试装计划（REQ-22）

已实际读取[FoldText评估与嵌入计划](../design-feedback/DF-20260828-002-foldtext.md)及用户提供的JSX/CSS。用户随后在REQ-23明确“直接安排实施，加入他们的任务列表里”，现已登记[实施队列](IMPLEMENTATION_QUEUE.md)并分别发送三个原任务。共享原生代码已存在，第一页/第三页已回执接收，第二页发送已确认但尚无接收回执；没有动效录屏，不把代码当成已看过效果。

- 顺序：统筹已在现有共享目录提供CSS3D＋Web Animations API原生适配；三页分别接一个指定固定标题，先看第一页1920×1080前后图和短录屏用于调整强度。此试装顺序不再是第二/三页取得实施授权的门槛；第二/三页结构改版、最新首页语音增量不为动画等待。
- 第一版试装参数建议：top、perspective=700、单片400ms、错峰20ms、折痕强度0.18、全段不超过800ms，只播一次；副标题默认静态。参数尚非最终视觉批准，缓动近似不冒称与GSAP数学等价。
- 保留原标题级别/ID与完整中文文本，继承共同字号/字重/字距/行高/颜色；字素、标点及数字单位混排要核对。没有JS或初始化迟到则保持静态，不能让已经显示的标题再次消失；中途开启减少动效也须恢复静态。
- 输入、选路、复制、下载不等动画；动态路径名、成品、风险、估计数字、MoneyAI来源、加载/错误/保存状态不加折叠。重复初始化幂等，销毁取消动画/监听并完整恢复DOM和样式，无永久will-change。
- 不运行附件npx，不安装React/GSAP，不换栈；原生适配不称原版已安装。统筹已读取[官方许可](https://github.com/DavidHDev/react-bits/blob/main/LICENSE.md)：复制/移植须保留归属和许可，不将组件本身或移植版单独再分发为组件库。本项目没有新增外发或部署授权。

共享模块为`title-motion.js/css`，页面显式调用`enhanceFoldTitle`，不全局扫描标题；无React/GSAP依赖。已有4项标题纯逻辑检查，最新完整套件见集成记录，真实动画、断行、DOM恢复及录屏未验，不能标视觉就绪。真实录屏沿既有Browser工具边界处理，不以HTTP、Node或静态图替代。

### 已有视觉与组件参考

来源：第一页任务的用户消息`01a046a2-1cd7-7170-b303-54981a729c94`。用户附图对应[候选1](../../../demo/assets/design/intake-directions-20260828/candidate-1.png)，指出仍粗糙、重复小字多、框体与控件过于模板化，并允许后续改进。**这是待改参考，不是“选1”或最终视觉批准。** 保留原三张候选、提示词与编号，不重新生成三套。

- PC主设计与验收视口改为**1920×1080**，取代原1440×900主稿；网页端完成后再做手机尺寸设计。1440×900、1280×720为PC补充回归，390px留到手机阶段，不用缩放旧图假装新主稿。
- “抖音做了不出单怎么办”是题目，不作品牌。此处原“名称待定”已被REQ-26覆盖：使用路芽，公共壳已加入文字品牌；不另造名或把IP概念原图冒充已获批准生产资产。
- 合并重复的起步说明，精简首屏辅助文字，减少不必要边框、模板按钮与说明堆叠；通过层级、留白和必要分隔组织内容。文件限制可集中说明，必要风险、来源、未知及保存失败仍须可见，不能为简洁删除真实性边界。
- 原图与原提示词保留生成时的1440×900目标和旧标题作为历史证据；按REQ-16先沿临时共同方向实现三页PC基础和必要状态，最终视觉由用户选择。

用户随后补充[上传区与描述输入两份参考](../../../demo/assets/design/references-20260828/README.md)，由第一页任务转交；新统筹实际查看并保留原图。最新[round-2合并稿](../../../demo/assets/design/intake-refinements-20260828/README.md)已显示，仍待确认。它的1920×1080是构图与后续验收目标，原始PNG为1672×941；未缩放冒充实际浏览器验收。

- 上传区参考分组、留白、拖放区域与选择文件的层级；不照搬英文暗色弹窗、URL导入、历史项目、PDF/DOCX/10MB或音频设置。支持格式和容量仍按共享契约，不执行附件内安装命令。
- 描述区参考圆角一体的聊天式输入及区内圆形提交控件；这仍是本轮问题描述，不是聊天记录或真实模型入口。整理为两个区域共用的单一主操作，不能另放一组重复提交按钮，也不能要求有文字才能处理材料。
- 中文字体重新考虑：用真实中文标题、正文、按钮、中文标点与69.90元、5MiB、1920×1080等混排检查字体家族、字重、行距、字形及回退。不得仅翻译英文后照搬字号间距；当前不冻结具体字体、不下载外部字体。
- 圆形提交控件须有明确中文可访问名称、可见焦点及至少44px触达区；鼠标、键盘走同一校验和忙碌守卫。中文IME组合/选字回车不提交，Shift+Enter换行；仅文字或已接收材料任一有效即可整理，空输入或实际忙碌时不可重复发起。必须在真实浏览器实测，不把延迟toast当处理成功。

## 产品体验

给不熟悉运营指标的商家一个能看懂、能做决定的经营助手。三页像同一个产品的连续三个步骤，分别解决“交什么”“选什么”“怎么做”，而不是三张长报告或三个功能密集后台。

| 页面 | 首屏焦点 | 次级信息 |
| --- | --- | --- |
| 1 说清问题 | 一句问题描述＋一个明确的材料投递区 | 收到的资料、可纠错整理、一个关键缺口 |
| 2 看清选择 | 路径之间的实际取舍与当前查看路径的结果/风险 | 可展开的来源、条件决策树、完整报告 |
| 3 拿去执行 | 所选动作的成品或步骤、一个主要取用按钮 | 使用边界、实验计划、可选反馈 |

不要大幅营销标题、装饰数据、雷达健康分、满屏卡片或三页都出现完整资料和分析。用户新增的聊天式输入只改变描述控件形态，不引入多轮聊天、AI回复气泡或未授权模型。没有确认的新卖点、收益数字或徽章不进入首屏。

## 必须一致的视觉要素

- 同一品牌位置、页头、三步导航、容器、字体层级、按钮、表单、状态提示、来源/风险标识；正式名称未定前不填造名。同一套设计token，不在页面CSS重新定义主题。
- 主要内容和动作要适合普通笔记本演示，正文优先16px以上、辅助信息不低于12px；按钮至少44px触达区，清晰可见的焦点样式。
- 1920×1080主稿与PC主验收，之后检验1440×900、1280×720；手机设计与390px回归后置。不横向溢出，不依赖仅hover才出现的关键说明。
- 色彩、留白、字号、边框与圆角由所选母版确定。浅色/深色未选，旧视觉与V0.4深色CSS都不自动成为目标。
- 动效用于投递反馈、展开、选路和状态变化，优先150–250ms，支持减少动态效果；不用无关粒子/光球掩盖信息。加载动画必须对应实际异步任务，不能模拟已经完成解析。
- 保持真实文字、按钮与表格可交互；设计截图不能整张当网页使用。图表是业务数据表达，不是装饰素材。

## 统一视觉的产生方式

1. 统筹使用用户指定的Product Design与Build Web Apps。按已安装skill读取上下文与约束；现有三张候选已交接，但尚无可直接实现的批准母版。
2. 接续现有候选及本节用户对候选1的反馈，形成修订参考并由用户确认；不要求重选已给过反馈的三张图，也不让三个页面Agent各自再开一轮主题投票。
3. 按REQ-16已沿round-2临时方向并行实现三页基础，不等待第一页打磨完；用户在实际PC页面上继续给方向，最终确认与基础实现分开。
4. 统筹已提取临时token、排版、公共壳、控件与状态，保存在共享目录。参考图保存在非敏感`demo/assets/design/`；不把临时token标为用户最终选择。
5. 每个页面Agent在首屏成形、关键布局变化、主要交互可用时实际查看并展示图像，说明参考/运行截图及真实尺寸，便于用户纠偏；重大风格变动由统筹协调，不自行分叉主题。
6. 先完成PC稿与基础主流程，再安排手机专门设计；用户允许后续使用Figma和优秀手机App参考，当前不创建手机稿、不另建工程或为此换栈。

用户明确要求三个对话接通21st与React Bits：先按[MCP约定](MCP_SETUP.md)实际验证检索，再作为组件/动效来源；不为使用它们而各改一套框架、安装无关依赖或替换用户已选设计。原三候选由Agent1使用ImageGen生成并保留；新统筹基于后续反馈生成单一方向的修订记录，没有重生同一组三候选或安装组件。

## 页面验收共同标准

使用Browser插件的应用内浏览器优先检查；本轮先记录1920×1080实际点击链、键盘、控制台与来源/风险可见性。将实际截图与临时参考并列审阅，注明参考原图1672×941、并非精确1920稿；最终目标获批后再做同尺寸比对。至少核对布局、字号、颜色、控件/间距、内容密度五项。当前可信路径错误已复现，Playwright回退需用户明确许可，不能改信任配置绕过。

每页写自己的QA记录：已检查、失败、未检查要区分。截图漂亮不等于业务流程通过，静态构建通过也不等于视觉一致。若环境缺少插件或浏览器，明确具体缺项，不捏造调用或验收记录。
