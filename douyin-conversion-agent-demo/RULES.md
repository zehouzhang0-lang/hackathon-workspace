# 开发规范

- 使用TypeScript，禁止使用隐式`any`。
- 单个源文件控制在300行以内，样式文件除外。
- 一个文件只承担一个主要职责。
- 页面只绑定`127.0.0.1`或`localhost`，不暴露到局域网。
- API Key不得进入前端、源码或Git。
- 真实数据、公开数据和合成演示数据必须明确标注。
- 不展示模型隐藏推理链，只展示证据、假设、建议依据、代价和风险。
- 不把规则Mock版冒充MoneyAI真实推理。
- 不顺手增加爬虫、登录、数据库和自动发布功能。
- 每个稳定切片必须构建成功并提交Git。
