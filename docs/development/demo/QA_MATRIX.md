# 三页与跨页验收矩阵

当前状态：**三页基础代码已存在，浏览器整项验收待执行**。这份表不是测试通过记录；已有纯逻辑与HTTP证据见[集成检查](QA_INTEGRATION.md)，不能替代整项UI验收。保留[原15项素材用例](../../../fixtures/acceptance.scenarios.json)。

2026-08-28更新：按[REQ-13—19](../CURRENT_BRIEF.md)，先1920×1080 PC与中文输入，随后PC补充尺寸；手机设计和390px检查后置。过程节点实际看图、给图。MoneyAI健康、分析返回、历史写入和读回分别验收；候选1及合并修订稿均未获最终视觉批准。旧页QA保留当时观察。

统筹提供可重复种子与失败注入入口；失败注入只用于测试，不伪装真实商家数据。页面Agent各自记录QA，最终端到端由统筹验收。

| ID | 操作 | 必须观察到 | 负责人 |
| --- | --- | --- | --- |
| D01 | 空会话只写一句问题；中文IME选字回车、Shift+Enter、按钮与键盘重复提交 | IME组合/选字回车不提交，Shift+Enter换行；所有提交入口同一校验并拒绝忙碌重入；无长必填表，确认范围后进页2有限结果，未知不补零 | 1＋2 |
| D02 | REQ-25下新图片拖入/选择/粘贴被关闭；已有历史图片读回、预览与删除；仅TXT/CSV/JSON材料开始 | 新图片入口不假装有图片理解；历史原件/状态不被升级或清空，材料仍可作为唯一输入，实际保存Blob/删除及失效一致 | 1＋共享 |
| D03 | 上传标准CSV/TXT/JSON；含带引号CSV/BOM | 确实读内容、保留来源行列；错误格式/超量/超限给准确失败，不丢其他材料 | 1 |
| D04 | 同轮逐次问/回答或跳过3题，跨页返回；更正第1题、刷新旧单问记录、新round | 一次最多1个asked，最多3个真实ID；跳过仍占预算，当前题未结束不问下一题，重复提交不重占额度。更正旧答案不丢后题；旧记录保留原ID/答案，加载不造保存；新轮才新预算 | 三页＋共享 |
| D05 | 载入合成主案例 | 三页同一个商品/窗口/来源，0支付不等于输入缺失；后续预编写反馈不提前出现 | 三页 |
| D06 | 提供不同时间、渠道或对象的数据 | 不拼漏斗、不给确定根因；只显示适用路径或不可估 | 1＋2 |
| D07 | 切换查看路径、展开证据和业务树 | 当前范围/投入/风险/结果解释联动；出处可定位；未知/未执行分支存在 | 2 |
| D08 | 查看情景测算并改允许的假设 | 结果与可复核算式一致；标注条件/未覆盖风险；不变成成功概率、实际上下界或增量收益 | 2＋共享 |
| D09 | 下载某路径报告后去看选择/执行状态 | 得到能离线打开的真实HTML，版本和完整树正确；只记下载请求，不自动选择/执行；动态文本不会执行脚本 | 2 |
| D10 | 选择路径进页3，不填反馈直接复制/导出 | 只给该行动所需执行包；复制成功有实据，失败可手动取用；执行仍unknown | 2＋3 |
| D11 | 明确“全部做了但变差”，实际执行时间留空 | execution=done、observation=worse、executedAt=null；不写declined、已达样本或已回滚 | 3 |
| D12 | 保存反馈，刷新，进入下一轮；重复点击 | 事务成功后才说本地保存；刷新实际读回；一个反馈只建一轮，旧执行与版本不变 | 3＋共享 |
| D13 | 页3返回换路或页1改资料，再返回 | 旧稿/报告标旧、不能当当前；需重新确认/分析/选择；不抹掉旧执行记录 | 三页 |
| D14 | 两个标签/迟到分析同时写，或保存/读取失败 | 拒绝旧版本/冲突写入，保留草稿并如实报错；不假装空会话或MoneyAI保存成功 | 共享＋三页 |
| D15 | 直达页2/3、浏览器前后退、退出未保存草稿 | 门槛和定位正确、不默认选路；有选择地保存/放弃草稿，返回不记未执行 | 三页 |
| D16 | 重置/删除附件；查看历史来源 | 明确确认、清本会话Blob；旧来源显示已删除，不从历史恢复已删除原件 | 共享＋1 |
| D17 | 先1920×1080主验收，再1440×900/1280×720；手机390px在PC基础完成后另验；真实中文、标点/单位、键盘、减少动效 | 共用当前临时方向与导航，最终母版批准单列；题目不作产品名，无冗余说明与不必要框体；字体回退/字重/行距可读，无溢出/裁切；图标按钮有中文可访问名、焦点及足够触达，风险不只靠hover | 三页 |
| D18 | 不联网/不启MoneyAI，从统一HTTP来源走手动或材料链 | 已标注本地有限演示可用；语音不可用时明确手动回退，不假称离线ASR；不发原文给未就绪提取服务，无伪造AI成功、控制台错误或404 | 统筹 |
| D19 | 核验MoneyAI健康、项目分析、决策历史写入与新会话读回；用第二个合成商家检验隔离 | 健康不代表业务就绪；专用会话/模型范围未配置时拒绝发送。真实结果经schema/版本校验才入状态，写入回执与实际读回各有证据，不自动进入个人历史 | 统筹＋后端 |
| D20 | 三页首屏、关键布局改动、主要交互可用时给用户看图 | Agent实际查看图像，标明运行截图/设计参考及尺寸；未验项可见，不拿静态图冒充功能。手机与Figma不抢在PC基础前推进 | 三页＋统筹 |
| D21 | 1920×1080、100%缩放检查第二页未选/已选/浏览另一条/旧版本/保存失败状态，以及第三页取用功能区；依据[DF-20260828-001](../design-feedback/DF-20260828-001.md) | 第二页首屏可辨两条示例路径、当前查看与已选状态、匹配状态的主操作及关键限制；浏览/切换详情/报告下载不改选择，明确保存成功才进入对应路径和版本的第三页。第三页成品/步骤选择、内容预览、就近复制/导出和自愿反馈分区清楚；复制对象与TXT整包范围标明，反馈不阻塞取用。不是长文平铺或逐段套框，关键风险常见，正文不靠缩小至16px以下挤入首屏 | 2＋3＋统筹 |
| D22 | 按[FoldText计划](../design-feedback/DF-20260828-002-foldtext.md)与REQ-23实施三页指定标题，先看第一页前后图/录屏调参；核对中文换行、迟到初始化、重复调用/销毁、减少动效中途切换 | 默认HTML静态可读，及时就绪才单次播放且全段≤800ms，不因迟到初始化重隐文字；多行或不可可靠定位时保持静态。关闭或中断完全恢复，功能和业务事件不等动画；三页实施已授权，首处实看不是重新批准开工的门槛，不称已安装原React组件 | 统筹共享＋三原页 |


| D23 | 首页七状态及真实语音：主动开始、计时、停止、重复点击、拒权/断线/不支持、改转写后导航保存与返回 | 单识别会话，实际麦克风与停止状态可辨；转写和手工文字分开保留，服务发送边界可见。失败可手动继续，不填合成转写；IME不误提交、保存失败不离页，原文未保存不误报已保存 |  1＋共享 |
| D24 | 九组确认经INTAKE_SET保存/读回：0与未知、老板判断、B→C→D连续更正、数组删除、材料替换、旧响应/丢回执 | 原文、编辑文、账本/来源、更正与完整投影原子一致；普通重解析不覆更正，合法新链可保存，旧派生值失效/未知。旧文件定位拒绝、同来源不重复事实；同次回答不自动二次INTAKE_SET，重试不造版本 | 1＋共享＋2/3回归 |
| D25 | 按REQ-26/28检查三页1920×1080默认态、具名展开/返回、路芽品牌及核心风险 | 仍是三页顺序任务，非NotebookLM三栏专业工作台或长Word；当前操作突出，关键风险/未知/来源可见，其他依据/假设/方案可发现且可返回。路芽文字统一，色值/IP使用未批准项不冒称定稿，不靠一键换色混淆错误和选中 | 三页＋统筹 |
| D26 | 提取服务未就绪、未同意发送、超时/取消/丢回执、非法结构、返回未发送文件B | 未ready/未同意不POST原文；失败保留可编辑草稿；已发但回执不明标发送未知。成功只能用本次实际发送材料的ID/version，确认前查会话/轮次/输入，不能把伪造或旧结果当MoneyAI成功 | 统筹＋1 |

## 每页交接格式

在自己唯一的`QA_AGENT_N.md`写：负责页面与契约版本、修改文件、启动来源、已运行命令/操作、结果、截图位置、未检查项、问题与共享模块变更请求。引用真实存在的文件；不要先写不存在截图的Markdown链接。

统筹最终检查：三页全部接同一状态API/样式；没有各自的localStorage键、不同合成商家、偷改共享文件或硬编码成功状态。实际验收记录与此待执行矩阵分开。

`python scripts/verify_demo_content.py`与`git diff --check`只检查仓库素材、文档链接和文本格式；`node --test demo/tests/logic.test.mjs`检查共享纯逻辑。它们都不能替代D01—D26；DOM/IndexedDB、下载、跨页和动效仍须在获准浏览器验证。
