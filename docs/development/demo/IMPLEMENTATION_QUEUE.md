# 当前已授权实施队列

更新：2026-08-28。由统筹`01a04697-8ecd-7663-8d03-a5d59511abdd`维护；这里列的是用户明确要求实施的工作，不是参考资料自动启动开发。任务发送、代码落地、运行验收分别记录。

## 优先顺序与唯一归属

| 顺序 | 已授权工作 | 负责人及当前依赖 |
| --- | --- | --- |
| 1 | REQ-25 首页语音入口增量：语音主入口、上传次入口、手动输入默认折叠，理解确认及最多三次逐问；与旧首页规则冲突处按新包，保留当前代码/第二三页/诊断和MoneyAI引擎 | 原第一页REQ-25四文件已固定并停写；统筹共享映射、多问历史与提取边界已接线。包为4份Markdown和1份JSON，不含可运行语音源码；当前只有纯逻辑/内存与HTTP证据，真实识别/浏览器未验 |
| 2 | REQ-20 第二页功能结构和查看/已选状态修订；第三页新取用工作区集成与PC回归 | 原第二页已发送但改版开工回执未到，已知选择/报告来源问题保留；第三页结构与REQ-25来源/导出回归已交接停写，统筹复跑8组组合通过，实际UI未验 |
| 3，可并行 | REQ-23 FoldText固定标题实施；不等重新批准开工，不阻塞语音、路径比较或取用 | 统筹共享增强器，第一/三页固定标题已接入，第二页待回执。真实画面尚未验证；先看第一页试装便于调参，不作为第二/三页取得实施授权的门槛 |
| 持续核心 | REQ-17 MoneyAI数据分析、路径与历史决策通路 | 统筹前后端；健康可达不等于业务接通，项目专用会话/模型/费用/材料范围及真实写入读回未完成 |
| 并行规范，随页面集成 | REQ-26 路芽名称、现有IP与品牌统一适配；REQ-27功能工作流参考 | 名称/IP已选，不重画；品牌与视觉反馈两个原任务交规范，统筹整合公共壳/PC，不能挤占语音和功能结构。原图入Git许可未确认，Figma只读连接不等于已设计 |

## 最新布局覆盖（REQ-28）

用户已撤回NotebookLM三栏工作台作为主布局，保留三页隔离、低上手成本和渐进展示。P1说清情况（语音/材料/按步骤核对）→P2看清并选下一步（短分析/路径/关键代价风险未知/选择）→P3拿内容使用（预览/复制下载/后置反馈）。完整依据、假设与其他方案有可发现可返回入口；关键风险、重要未知和MoneyAI来源不可藏。豆包只是方向描述，不新增无限聊天或声称已取得其截图。已通过原任务消息同步，不把发送当成实施完成；页面定点修改随共享接口和PC证据推进。

## FoldText实施清单（已授权，真实动画未验）

- 统筹：已提供`demo/shared/title-motion.js`和`title-motion.css`原生增强器；未安装React/GSAP。显式`enhanceFoldTitle(heading)`返回含`status`、`reason`与幂等`destroy()`的控制器；仅接带`data-fold-title`的固定h1/h2，每文档最多一个。页面在业务异步初始化前调用，不等待动画完成。
- 第一页：在自己的四文件中为固定主标题接入，不动画输入/材料/纠错。新增语音入口优先，标题动效跟随这次标题调整；提供1920×1080单标题前后图和短录屏，通道不可用时如实记录。
- 第二页：在原有五文件内给精简后的固定页题接入，禁止动态路径名/风险/估计数字/分析来源动效；结构、选择状态和报告问题优先。
- 第三页：固定`delivery-title`“行动内容”已实际接入，原任务交接停止写入；不动画动态行动名、成品、保存状态。仅早期初始化登记的隐藏标题可等首次显现，迟到初始化静态跳过，不让取用等待动画。
- 参数共用：400ms/片、20ms错峰、总预算≤800ms、top/700px/折痕0.18、仅一次，副标题静态。迟到/多行/字体未就绪/缺API静态；减少动效中途变化、销毁、标题变化都恢复原文本，原DOM与业务数据不被替换。
- 当前验证：已有4项标题预算/字素/回退/无DOM测试，最新全套运行见集成记录；没有真实动画、断行、DOM恢复、朗读或帧率证据。

参考与许可：[FoldText评估](../design-feedback/DF-20260828-002-foldtext.md)、[视觉简报](DESIGN_BRIEF.md)、[D22验收](QA_MATRIX.md)。原生适配不是原版组件已安装，不执行附件命令，不静默切换浏览器。

## 首页语音入口实施清单（最高优先）

新包SHA-256：`0453067ec615a6a3270c26b96b4b3f802818bb6765c1fdc1fa8f2f7da74498d8`。只读核查5项CRC通过、总展开20790字节，无穿越/绝对路径/符号链接/可执行项；原件不复制入仓库。包内旧仓库/React环境说明不取代实际工作区和原生HTML技术基线。

- 第一页：等待、听取、转写、提取、理解确认、一次一问、确认后分析七状态；真实计时、停止、单识别会话，中文转写可改。语音不支持/权限拒绝/断线保留文字并回退手动；不使用预置文本冒充转写。
- 第一页＋统筹：商品、价格规格、平台、人群判断、场景判断、问题、已做动作、限制和数据/缺口进入确认内容；TXT/CSV/JSON进入确认。没有真实图片理解时关闭新图片上传入口，旧图片原件/历史不删除。
- 统筹：新增草稿校验和单一映射；原文、来源、老板判断、纠错及未知分开，确认不等于外部核实。`productClicks`不能等同详情访客，`createdOrders`不能等同支付订单，口径/窗口不符不拼漏斗。
- 统筹：同轮最多三问且一次一问的共享实现已落地；questions历史、activeQuestionId、remaining和旧单问只读兼容已交第一页，已纳入Node回归。原始ID/答案保留、编辑不重发额度；事务幂等/跨标签与页面实际操作仍待浏览器，不称已全部验收。
- 统筹后端：/api/intake/extract、客户端及严格JSON/超时/可编辑回退已落地；当前真实返回409 intake_unavailable，未发送给MoneyAI。服务尚未接通，原生浏览器语音另待实测，不伪造提取结果。
- 第二页：消费已确认输入，修正多问来源定位和报告校验，保留人群/场景判断标签；不借此次重写诊断引擎。
- 第三页：新输入导致的旧选择/产物失效、来源与导出限定回归已交回；根任务独立执行8组reducer组合通过。来源从分析快照读取，TXT不扩大原文/问答导出；不增加语音入口，不改变先取用后反馈。
- 隐私：采集仅由用户主动开始并可停止；不保存音频不代表音频不出设备。浏览器识别服务/发送范围、麦克风许可和MoneyAI文本发送/模型费用分别确认；不自动接任意云ASR、不自动安装语言包。

## 独立设计任务

- “视觉反馈与动效评审”`01a046ec-d06b-75b3-85fe-e02a9c9c9a34`继续接收页面/动效参考，独占反馈目录，不改应用或派活。
- “产品 IP 与品牌设计”`01a04706-a4a9-72e3-b13f-c4fdb78bda1b`已交[三份品牌规范](../brand-ip/README.md)并停止写入；路芽名称/现版IP/理念已定，色板/字号/具体措辞/静态使用仍为建议，原图入Git许可待核对。它不改页面/Git或给三页派活。

## 检查与同步

1920×1080 PC先行，移动/Figma后置；最终视觉仍由用户决定。Browser可信路径错误和备用浏览器许可未解决，现有Node/内容/HTTP检查不代替UI/语音/动效验证。各页完成后交QA并停止写入，统筹只按实际核对过的指定文件统一Git；不能以本清单已登记声称代码或验收完成。
