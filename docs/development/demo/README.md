# 三页 Demo：并行开发指令入口

更新：2026-08-28。三份指令已交付，用户已确认最小Demo并下发，三页前检完成、页面未实现。随后用户在“第一页开发agent”授权统筹准备，该任务正在生成三张视觉候选并补两份独占准备文档。**新统筹应接收这批进行中成果，不重复生图或再次下发页面。** 责任与迁移进度见[接续记录](COORDINATOR_HANDOFF.md)，恢复提示词见[可复制提示词](RESUME_PROMPT.md)。

## 一眼看懂分工

| 负责人 | 页面与唯一主目标 | 不在本页做 |
| --- | --- | --- |
| Agent 1 | `01-intake.html`：说清问题，收材料，核对后确认本轮范围 | 原因诊断、路径推荐、经营大屏 |
| Agent 2 | `02-decisions.html`：比较经营路径、结果范围、证据与风险，下载报告并选路 | 重新收完整资料、生成整套执行内容 |
| Agent 3 | `03-action.html`：拿走所选行动的执行包，自愿记录执行与观察 | 重做分析、任务看板、自动发布 |
| 接续记录指定的统筹 Agent | 统一视觉母版、共享代码/案例/状态契约、最终串联与Git同步 | 不与页面Agent同时改同一个文件 |

本轮授权例外：Agent1先完成已开始的三张视觉候选、`CONTRACT_DETAILS.md`和`SHARED_BUILD_PLAN.md`；迁移中的统筹不写这两份文件、不重复生成候选。已有基线/公共契约/Git仍由迁移统筹统一处理，共享代码开工前明确唯一写入人。

用户已明确三页隔离、统一风格、职责清晰、同仓库并行，并以“可以，我已经放手让他们去开发了”接受上一轮最小Demo提案、确认下发。以下原生HTML、单选路径、HTML报告和第三页最小交互作为本轮演示基线；不等于全部生产产品问题、视觉、统计算法或外部接入均已定案。

## READY：唯一开工状态

| 门槛 | 当前值 | 谁能更新 |
| --- | --- | --- |
| PROMPTS_READY | 是；三份prompt、公共契约、文件归属与链接已检查 | 只表示指令完整，不表示视觉/应用/浏览器验收完成 |
| DEMO_SCOPE_APPROVED | 是；限本轮三页最小Demo | 依据上述用户确认；不扩大为视觉或外部数据授权 |
| VISUAL_APPROVED | 否 | 用户选定统一视觉母版后由统筹登记，附相对文件路径 |
| SHARED_READY | 否 | 统筹实现并验证共享底座后登记，附契约版本和测试记录 |
| MCP_PREFLIGHT | 三个页面对话均实际检索通过 | 见各页QA；新设备/连接仍需按[MCP约定](MCP_SETUP.md)实测，已通过者不重复安装 |
| AGENTS_DISPATCHED | 是；三页均已接收并写前检QA | 任务ID及真实状态见[接续记录](COORDINATOR_HANDOFF.md) |

页面实现条件：Demo范围获批＋视觉已选＋共享底座可用＋该页面任务已下发。未满足时可读文档和检查依赖，不能自行补一套共享库、选另一套主题或伪报完成。这是先做一次共同准备，再真正并行，不是让三个Agent互相等待对方。

## 本轮建议的实现取舍

- 三份独立HTML，共用CSS和原生ES modules；不初始化三个React工程。功能在本地浏览器真实可交互，生成内容注明演示来源。
- 用现有Python提供同一来源的本地HTTP服务；不依赖`file://`双击运行，不依赖CDN、云数据库或MoneyAI才能看Demo。
- 先真实实现文件接收/预览、受支持文本解析、页面跳转、状态保存读回、文案复制及HTML报告下载。
- 任意图片OCR、抖音后台抓取、真实模型诊断、统计预测和MoneyAI长期记忆不在本轮默认实现范围。图片未识别就写未识别，演示模板不能冒充实时模型输出。
- 第三页采用“只交付所选行动所需内容＋可选轻量反馈＋明确保存＋带反馈回第二页”的最小演示范围；不再追问是否应强制填反馈。
- 视觉先统一：不直接沿用被否决旧稿，也不因为V0.4已有CSS就采用其深色风格。

## 开始开发的顺序

1. 三页分工与本轮最小范围已登记，三个开发任务已下发；不新建重复页面任务。
2. 接收Agent1已获准生成的视觉候选，依照[视觉简报](DESIGN_BRIEF.md)与用户选定共同目标；使用Product Design与Build Web Apps，三页不能各选风格或重复生图。
3. 接收Agent1委派的两份接口/底座准备文档，统筹整合[共享契约](SHARED_CONTRACT.md)，明确唯一实现人，再交付`demo/shared/`、合成示例、公共壳和冒烟测试。记录SHARED_READY的实际验证证据。
4. 统筹通知现有三个开发任务依赖已就绪，继续对应prompt。三个对话只改各自文件，共享文件只读；下面的初始指令保留作备查。
5. 每页分别验收与交接；统筹执行[跨页检查](QA_MATRIX.md)，统一提交和同步。未验证的能力如实列出。

### 直接复制到三个对话

**对话1：**

```text
请读取根目录 AGENTS.md、docs/development/demo/README.md，
再执行 docs/development/demo/PROMPT_AGENT_1.md。
严格遵守共享契约、READY状态与文件归属，只做第一页。
不要初始化另一套工程、修改共享底座或提交其他人的文件。
```

**对话2：**

```text
请读取根目录 AGENTS.md、docs/development/demo/README.md，
再执行 docs/development/demo/PROMPT_AGENT_2.md。
严格遵守共享契约、READY状态与文件归属，只做第二页。
不要初始化另一套工程、修改共享底座或提交其他人的文件。
```

**对话3：**

```text
请读取根目录 AGENTS.md、docs/development/demo/README.md，
再执行 docs/development/demo/PROMPT_AGENT_3.md。
严格遵守共享契约、READY状态与文件归属，只做第三页。
不要初始化另一套工程、修改共享底座或提交其他人的文件。
```

完整prompt：[Agent 1](PROMPT_AGENT_1.md) · [Agent 2](PROMPT_AGENT_2.md) · [Agent 3](PROMPT_AGENT_3.md)。

## 本机环境与同步

已实际检查：Windows PowerShell；Python 3.13.9、Node 24.14.0、Git可用。21st与React Bits在三个页面新对话均检索通过；旧统筹连接的历史错误不再阻塞它们。详见[MCP记录](MCP_SETUP.md)。尚未安装组件，Product Design没有已批准的视觉母版。

共享底座建立后，从仓库根目录运行以下命令即可提供静态页面；**当前demo目录仅有未批准的设计候选，没有三页或共享代码，服务未启动**：

```powershell
python -m http.server 4188 --bind 127.0.0.1 --directory demo
```

只服务`demo/`，不暴露仓库根目录及`.local-inbox/`。三页使用同一个`127.0.0.1:4188`来源，否则本地存储会隔离；端口被占时由统筹统一调整，不让各Agent单独换端口。切勿结束未知进程来抢端口。

同目录并行期间，页面Agent不执行pull、checkout、reset、commit、push，不改根README/AGENTS/进度/依赖锁文件。统筹在合并检查后同步一次；不得`git add .`误带队友进行中的文件。GitHub同步代码和文档，不同步浏览器商家数据、登录或MCP凭据。

## 本轮吸收了什么

见[V0.4复用边界](V04_REUSE.md)：吸收实验计划、事实与假设区分、模块划分和测试素材经验，不把其固定漏斗、质量分、关键词反馈、双模式UI当成我们已确认需求。原件与私密审查记录保持本地，不作为新对话必须读取的路径。

DEV-001旧指令保留为历史，当前入口由本指令包取代；没有执行DEV-001，也不要求页面Agent先重跑一轮旧文档任务。
