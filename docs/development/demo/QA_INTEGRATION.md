# 统筹集成检查

2026-08-28，现任统筹“路芽开发统筹接续”`01a0476a-6049-70d2-9bb0-95af778428eb`；下文旧轮明确保留为历史。三页基础代码已落地，**未完成整项浏览器验收、最终视觉或MoneyAI业务验收**。以下与历史前检分开；页面交接分别见[QA1](QA_AGENT_1.md)、[QA2](QA_AGENT_2.md)、[QA3](QA_AGENT_3.md)。

## 接任后本批：更正一致性与MoneyAI客户端边界

接任提交`b2187f95594b7bc7c7c055c3017974a1b23fdedf`已实际核对本地/远端一致。不是再次等待正式移交；继续既有REQ-25与三页职责，不纳入尚未确定的新会议提案。

| 检查或改动 | 本任务实际结果 | 边界 |
| --- | --- | --- |
| 更正依赖 | 独立复现FACT_PATCH后旧派生值仍进入分析；先加失败回归，再修复为递归未知/撤销旧条件，保留根更正值、源历史和无关事实 | 仅合成reducer与分析快照，未运行IndexedDB |
| 文件确认一致性 | 独立复现文件订单0→更正5后卡片仍current/快照0与5冲突；现标stale，旧稿拒绝，同ID恢复与继续更正/未知通过，显式新B绑定不被旧A更正抢占 | 不改外部fact的intakeField归属，P1页面恢复续修在原任务进行 |
| MoneyAI客户端 | 严格状态、默认发送许可门槛、能力检查、固定JSON正文、256KiB、无重定向、取消/超时与发送三态已修；HTTP或未校验回包不充当分析 | 6项fetch替身检查，无真实模型/业务请求、无个人历史 |
| Node当前套件 | 53/53通过；新增两项文件恢复/新绑定回归，扩展依赖测试为INTAKE_SET及FACT_PATCH两路，并加入6项MoneyAI边界测试 | 不证明页面UI、IME、语音、浏览器存储、下载或模型效果 |
| Python后端 | 当前文件重新执行12/12通过 | 临时本机测试端口/模拟健康回包，不是MoneyAI业务接通 |
| 本机预览 | 上一轮服务已停止；确认4188无监听且无该服务进程后，以原工程恢复统一服务。实际GET首页/health/status成功，MoneyAI健康可达而四项业务ready为false | Browser初始化仍报Trusted RPC可信路径错误；未修改信任或使用未获许可的备用工具 |
| Lovable收件 | DF004与反馈索引已实际读取、哈希/链接核验；仅提示词和索引 | 未运行Lovable、未上传私人IP/材料、未换工程或批准视觉 |

原P1已确认接收文件来源更正恢复的定点续修；原P2旧轮active、新回执未到，已向原任务发送本批合并待办。P3、视觉反馈、品牌原任务保留且本批停写。统筹不覆盖页面在途文件。本任务已在本批文件上实际重跑1546项内容检查/15个素材路径、18份JS语法及git diff --check；这些是静态检查。指定文件的暂存blob与Git同步另行核对，不纳入尚未交回的页面在途改动；SHARED_READY与VISUAL_APPROVED仍false。

## 既有页面反馈（历史）

后续用户提供第二页实际截图（1891×955原PNG，非精确1920×1080），统筹已实际查看：上下文/说明占据首屏大部、路径进入画面较晚，主操作在本画面未见，不能断言整页没有按钮。图中已有明确选定提示，不能当成未选初始态。用户同时批评第三页，但本次没有第三页截图，不将其写成已目视验证。REQ-20结构改版已下发原第二/三页，以下已有检查不能充当改版后的验收。

“视觉反馈与动效评审”首轮四文件已实际读取/核对，见[DF-20260828-001](../design-feedback/DF-20260828-001.md)。统筹独立核对原件与保存副本均为110314字节，SHA-256均为`9f2e414db6b7bf1073db659d75bba311b00ac0dcb6f0f2ec0c3d63ed9dc49aed`；原图未改。三项结构建议及查看/选定区分已整合至视觉简报与D21，并补充下发原两页，不视为实现或视觉验收。此轮任务快照：第三页已交回功能工作区四文件并停止写入；第二页工具仍显示上一轮收尾active，未收到改版开工回执，不能报两页均已改完。

## 上一任集成快照：REQ-25/26/28与磁盘恢复（历史）

2026-08-28，上一任根任务的检查如下；45项/12项及后文早期25项/8项等数字均是对应轮次的历史，不代替顶部当前检查。

| 检查 | 根任务实际结果 | 仍未验证 |
| --- | --- | --- |
| Node统一逻辑套件 | 45/45通过；含三问/旧单问兼容、INTAKE_SET原文与编辑文一起保存、连续更正/数组删除/断链拒绝、文件定位复用、传递派生失效及提取来源发送边界 | 无DOM、真实麦克风/ASR、IME、IndexedDB事务或跨标签结论 |
| Python后端套件 | 12/12通过；保留Host/Origin/JSON/大小/私密路径限制，新增提取未就绪不发送及提前拒绝请求有界消费正文 | 使用本机临时测试端口/合成请求；不是MoneyAI业务或真实音频验证 |
| 内容与差异 | 内容脚本1509项通过、15个素材定义路径有效；git diff --check退出0 | 不验证UI或业务能力，不覆盖未跟踪内容的全部安全审查 |
| 统一4188服务 | 实际GET三页HTML、intake-draft/extraction、shell/base均200；MoneyAI健康可达，analysis/history write/history read/extraction均未就绪；合成POST提取真实409/intake_unavailable/editable:true/sentToMoneyAI:false | HTTP200不是页面渲染、保存或取用成功 |
| 公共文字品牌 | shell加入“路芽”、三步任务用语和页面标题，保留现有字体/颜色；JS语法与HTTP通过 | 未做实际1920截图或视觉批准；未使用私有IP原图或一键换品牌色 |
| 共享浏览器宿主定义 | 13项用例定义，语法、导入、挂载点与空会话防护已静态核对；含INTAKE_SET原子读回/abort/失联重试与三问历史/额度 | 实际浏览器及IndexedDB运行0项；旧单问仅内存兼容，未作旧库升级或刷新验证 |
| 任务交付 | 原第一页REQ-25四文件已固定并停写，全部哈希已由根任务核对；原第三页来源/导出限定回归已交回并停写；第二页仍无新回执 | 基础交付不等于UI验收；第二页已知问题不由统筹竞争写入 |

共享修复保留边界：有序原文/账本/更正不排序；自动投影不能覆盖已有更正；合法新增链须从当前值出发，删除有引用则未知墓碑。输入改变时旧派生值递归退出已知状态，原ID/来源与旧值历史保留，不重算虚假数据；提取回包不能引用本次未发送的材料。原本两个更正用例失败，修复后全套通过，失败没有计作通过。

第三页REQ25来源修补已交回。根任务实际读取新formatter/分析快照引用，并复跑该QA的8组reducer组合全部通过；在既有统一TXT用例补入text/txt/CSV定位、判断/冲突标签、只取分析快照且不扩出quote的检查，全套仍45项通过。外部FACT_PATCH产生草稿A/当前事实B的恢复，根任务另验无/有已存前缀两组：A→C被拒绝，用户明确核对后B→C一次保存成功，原历史保留；页面恢复已交第一页处理，尚不代表UI验收。

随后根任务在第一页源码SHA-256 `855ebeb91e25d697cfa560185067655560af9788c136d83e5f34ee696f244245` 上，提取实际saveIntake.finish/attempt/retryPending/applyState及connect恢复条件，搭配真实reducer与内存替身复验8组通过：正常/无变化保存可续行；提交后再加材料的旧回执保留dirty与原文、不自动续问；纯revision冲突只更新expectedRevision并保持原ID/载荷，业务IV变化则拒绝；空intake的示例/重置反复应用会清理干净旧理解，有脏稿保留。没有运行DOM、真实BFCache事件或IndexedDB，提取/ReferenceError不计作通过。最终页面交付与hash以QA1为准。最终JS为125378字节、SHA-256 `4b854d917c9652cb4d132b4b97f6d85baecdd2dcb9de1576095fe2bb6a8872ca`；原作者说明仅再修正一处尚未保存的提示文案。根任务在最终四文件固定后，重新运行45项Node、12项后端、1509项内容、18份JS语法与差异检查均通过，8个HTTP页面/模块资源返回200且响应字节与磁盘一致；上述8组闭包探针对应此前明列的源码快照，不把原作者其他内存计数冒称根任务独立执行。

**磁盘事件已恢复，但保留防护。** C盘曾为0，intake.js及server/test_app.py被写入失败截断；已从任务源码记录/备份恢复并通过语法/对应逻辑检查。最近根任务C盘只读值约7.05GB，不再沿用“仍0字节不可用”的旧状态。写入继续每批检查>1GiB、D盘留存、同目录临时文件flush/读回/原子替换；不清理用户原件。恢复副本位置只在任务消息保管，不入应用或Git。

已实际读取品牌三文档与DF003最新REQ28记录。路芽名称/现版IP/理念已定；NotebookLM三栏主布局撤回，三页按当前任务渐进展示，必要风险/未知/来源常见，其他完整信息有具名入口。色板/字号/IP静态放置仍为建议，不以文档交接或参考图冒充UI验收。

## 早期已执行检查（历史）

| 检查 | 实际结果 | 不能推出 |
| --- | --- | --- |
| `node --test demo/tests/logic.test.mjs` | 25项通过、0失败；16项共享、4项第一页及5项第三页预览/整包/反馈与第二页报告组合。新增覆盖内容/步骤文本、逐次TXT同意/BOM/CRLF/两份产物、输入失效、done+worse/未知日期与浏览另一条报告不改选择 | 不证明DOM、真实IME、剪贴板、下载落盘或IndexedDB事务通过 |
| `python -X utf8 -m unittest discover -s server -p 'test_*.py' -v` | 8项通过；本机HTTP测试、Origin/JSON/大小/私密路径限制、健康与业务状态分开、未配置不发送 | 测试内健康为模拟返回；不是MoneyAI业务调用 |
| 本轮独立只读代码复核 | 连续更正后重解析不重复旧值；递归撤销依赖；坏UTF-8/图片解码失败在写事务前拒绝。未再发现阻断问题 | 注入解码失败和内存复现不等于真实浏览器图片/事务验证 |
| 统一4188服务HTTP检查 | 三份HTML及共享模块响应200；`/api/health`成功；`/api/moneyai/status`实际本机服务可达且三项业务ready仍false | HTTP成功不证明页面显示、交互或历史正确 |
| JavaScript语法 | 共享核心和三页模块`node --check`退出0 | 不证明浏览器事件、模块生命周期或可访问性 |
| 三页静态集成复核 | 资源/导入路径、共享导出、必要DOM挂载点与无重复ID检查通过 | 未运行浏览器，不证明渲染或交互 |
| 设计素材 | 原三候选与两轮修订原图保留，签名/尺寸/哈希已核对并实际查看；round-2原图1672×941，目标1920×1080 | 不是三页运行截图，也不是精确1920视口验收 |

仓库内容检查与`git diff --check`在最终同步前另行重跑，计数以实际最终命令为准，不用页面Agent历史计数充当统筹最终检查。

本次DF交接整合后，统筹重跑`python scripts/verify_demo_content.py`为1335项内容检查通过、15个验收定义路径有效；`git diff --check`退出0，20项Node及8项后端边界测试再次通过。此为改版进行中的检查快照，不是最终Git快照或UI验收。

### 第三页REQ-20结构交接后的集成

- 已实际读取新HTML/CSS/JS及本页QA。页面具有内容选择栏、单份内容/步骤预览、就近复制与手动选取、整包TXT、独立风险/使用条件和后置可选反馈。只读代码复核未发现本批范围内可确认的阻断缺陷，仍须真实点击和截图验证。
- 在已有测试文件追加5项组合回归并实际运行，累计25项通过；不能以原作者148项内存断言或此处纯函数结果声称真实浏览器成功。
- 公共壳原上限1440px与第一/三页1728px工作区不一致。统筹仅调整共享宽度token和通知区域：PC正文按90%/1728px上限，页头/页尾包含左右各24px内边距。1920×1080的对齐是待截图核验的目标；较小PC断点和中文排版仍未目视检查。
- 本批保留公共API、事件语义、每次导出同意与反馈后置；没有新增模型、MoneyAI历史、自动执行或手机/Figma能力。

## 第二页改版中的已知问题

2026-08-28只读代码复核，已发送原第二页任务；在其交接修订前保持未解决：

- 已选A、当前查看B时，列表只显示“正在查看”，未独立指出A仍为已选。需保留两个不同状态，并在操作区清楚交代；当前浏览和报告生成本身不更改已存选择。
- 当前已选有效路径的“继续准备”仍调用一次`PATH_SELECT`。共享层不会重复选定事件，但新增回执写入失败会阻止继续。应重读校验当前分析/选择/版本后直接导航，只有真正选定或改选才发保存命令。

以上来自`decisions.js`当时的`renderPathList`与`choosePath`，不是浏览器目视结果，也不是已修复声明。

REQ25仍待原第二页回执：report.js的补问来源校验只读单个clarification.questionId，尚未改用questions历史/analysis.clarificationSnapshot；页面与报告的来源格式化尚未覆盖intake及txt字符位置，尚无owner_hypothesis/confirmed_fact明确标签。这些已下发，不由统筹覆盖其在途文件。2026-08-28任务工具仍返回旧active轮次，后续开工/修复未收到，不能把原基本报告测试充作新多问来源或REQ28布局验收。

## 修复范围

- 实质修改输入/反馈后清除全合成fixture标记；保留各事实的真实来源，不把新增商家描述当合成答案。
- 更正值通过历史原始来源保持同一fact ID，重解析不将旧值重新放到前面；删除/替换或答案改未知时同步撤销派生事实及依赖约束。
- 过期FACT_PATCH拒绝恢复被删除事实；坏文件在替换写事务前拒绝。迟到实质输入变更原子失效确认、分析、选择和产物。
- 页头导航、载入示例与清空共享草稿守卫；onDiscard=false与保存失败都阻止离开，不把页面拒绝当成功。

## 未执行与下一轮次序

1. **1920×1080三页真实截图与主流程**：首屏、中文字体、IME/Shift+Enter、材料独立起步、键盘、忙碌/错误态。过程截图供用户实时给方向，最终视觉单独确认。
2. **真实IndexedDB/Blob**：保存后刷新读回、订阅/跨标签冲突、事务abort、提交后响应丢失及原commandId重试、删除/替换/跨轮历史、存储不可用。不以纯reducer测试替代。
3. **真实取用与反馈**：剪贴板成功/拒绝，HTML/TXT实际下载与安全内容、BOM/CRLF，单次摘要许可、日志补存不重复操作；done+worse+executedAt=null，未反馈仍unknown。
4. **PC补充尺寸与手机阶段**：先1440×900/1280×720；PC基础稿与主流程可用后才做手机/Figma专门设计与390px验收。本轮未进行手机验收。
5. **MoneyAI业务通路**：专用项目会话、获准模型/数据/费用范围、分析校验、决策写入与独立读回、跨商家隔离；当前仅健康可达，详见[集成记录](MONEYAI_INTEGRATION.md)。

## 浏览器阻塞

[共享测试宿主](../../../demo/tests/shared.html)及脚本已扩展并通过JS语法检查，13个用例定义**尚未在浏览器执行**。INTAKE_SET涵盖原文/编辑文/来源/更正一起保存、提交前abort和提交后失联原ID重试；三问涵盖顺序、旧答案、额度与新轮。旧单问兼容仅内存检查，不代表旧库升级。只允许独立空测试浏览器配置，必须点按钮开始；检测到既有保存记录拒绝执行，不清除用户数据。页面主流程、跨标签、实际下载与截图仍需另验。

三个页面任务及统筹均尝试已安装Browser插件，初始化前报`Trusted RPC dependency must resolve within a configured trusted code path`，未获得浏览器实例。没有修改信任配置或静默换工具。统筹已向用户询问使用本机已有Playwright作本地合成数据备用验证的许可，尚无明确答复；图像/PC优先要求不自动等于该工具许可。

应用预览请求已送入Codex面板，但queued不等于已打开或已查看。任何后续截图须标真实视口与状态，不用参考图或HTTP结果冒充运行截图。

## 基础范围差额

当前第三页只读成品、先取用再自愿反馈；复杂编辑、完整历史导出、反馈修订和指标表单未完成。第一页九组理解、最多三问和原文保存已接线，根任务已验纯逻辑；真实语音、逐问操作、刷新与持久化仍另验。任意OCR、平台接口、真实模型、统计预测和经营效果未验证。`SHARED_READY=false`与`VISUAL_APPROVED=false`保留。
