# 第二、三页当前测试版本

更新：2026-08-28。以本文件所在提交为准。本批发布现有可运行代码，不代表全部PRD、真实浏览器或业务验收完成。

## 队友立即更新

仓库已公开，可匿名浏览和克隆。已有项目先查看本地修改；有修改时先自行保存或提交，不使用强制覆盖。

```powershell
git status --short
git pull --ff-only origin master
python -B -X utf8 server/app.py --port 4188
```

在仓库根目录运行；如果4188已被自己的该项目服务占用，保留原服务并刷新页面，不再启动第二份。若占用者不明，不杀进程，改用空闲端口并在后续访问中统一替换。

尚未克隆的队友可先运行：

```powershell
git clone https://github.com/zehouzhang0-lang/hackathon-workspace.git
cd hackathon-workspace
```

然后运行上方启动命令。无需个人MoneyAI账号、MCP配置或任何凭据。使用自己浏览器打开 http://127.0.0.1:4188/01-intake.html ，不要用file方式直接打开HTML。

## 最快看见第二、三页

1. 在页面顶部“选择合成演示案例”中选择“合成案例 · 榨汁杯”，点击“载入示例”。这是公开合成数据；不要上传真实商家材料来代替本轮测试。
2. 在第一页核对资料并显式确认进入分析；如出现可跳过的补问，按页面提示处理。旧会话先回第一页载入新案例，不要求清空浏览器全部数据。
3. 第二页核对来源、日期、缺口和五阶段漏斗；查看“优先验证”与事实／假设／未知的区别。首轮A是补全首屏购买判断，B是制作真实问题验证内容。查看报告，再明确选择其中一个方案。
4. 进入第三页，查看已选方案的内容、清单与实验卡。可尝试复制／TXT下载；采用、执行应分别记录，初始保持未知。测试反馈仅填写虚构数据，保存和读回失败请保留提示。

第二页地址：http://127.0.0.1:4188/02-decisions.html

第三页地址：http://127.0.0.1:4188/03-action.html

直接打开后续页面若提示缺少资料、分析或选路，请从第一页完成前置步骤；这不是预先伪造结果的静态展示页。

## 本次范围与尚未接通

| 部分 | 本次代码范围 | 明确边界 |
| --- | --- | --- |
| 第二页 | 来源／时间／缺口、漏斗与验证优先级、A/B比较、判断反馈、明确选路、决策树及报告导出；版本冲突可重新核对 | 本机合成分析不等于真实MoneyAI分析，也不证明因果或建议有效 |
| 第三页 | 已选稿件预览与取用、清单、实验ID／假设／计划、采用与执行分开、v1反馈明细、版本回执保护、历史记录入口 | 剪贴板、下载落盘、IndexedDB保存与读回仍需队友实测 |
| 共享层 | 新首轮策略、严格v1反馈、反馈后再判断和显式接受候选建轮的逻辑及幂等保护 | C7／C8共享代码已存在，但第三页相应按钮尚未接线，不宣称完整闭环可操作；C6反馈附件事务未发布 |
| 首页兼容 | 保留队友提交的截图与XLSX/XLS原件接收 | Excel解析、截图OCR与真实自动整理未接通；收到文件不等于提取成功 |

真实MoneyAI业务调用／历史写入读回、真实语音、跨标签／刷新／Blob、1920×1080视觉和动效仍未验收。本机浏览器工具受可信路径问题阻塞，没有绕过权限或用参考图冒充运行截图。

## 检查与反馈

发布候选已实际通过：共享Node 110项、第三页PRD合成检查8项、Python后端12项、21份JS／MJS语法；内容检查单独通过。以上不是UI或真实存储测试。

```powershell
node --test demo/tests/logic.test.mjs
python -B -X utf8 scripts/verify_demo_content.py
python -B -X utf8 -m unittest discover -s server -p 'test_*.py'
```

请使用[完整测试与反馈指南](README.md)中的反馈表；旧版本表以本页为准。提交Issue时写明commit、浏览器／分辨率、案例、步骤、预期、实际和截图；不附个人材料、令牌、Cookie或浏览器配置。

开发接口与未完成项见[最新PRD执行基线](../development/demo/PRD_V1_EXECUTION.md)。团队修复请保留现有改动、提交可复现测试，不强推master。
