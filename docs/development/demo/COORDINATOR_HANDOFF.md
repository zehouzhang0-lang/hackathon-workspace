# 统筹接续记录

更新：2026-08-28。用户张泽厚要求缩短上下文，在新任务恢复统筹，验收接管后迁移本项目全部待办。这里移交的是需求、协调、共享开发、验收和Git职责，不是迁移其他项目、浏览器私有数据或旧对话的内部思考。

## 1. 当前责任与迁移状态

- 状态：`PREPARED`；尚未通过新统筹接管验收，旧统筹仍负责本次迁移。
- 原统筹任务：`接下来我要参加一场黑客松，我需要你帮助我`，ID `01a04257-8ff6-7000-a7df-17ed818f8183`。
- 新统筹任务：待创建并登记；在状态改为`ACCEPTED`且收到正式接管消息前，仅做只读验收。
- 用户最新指令：“可以，我已经放手让他们去开发了”；随后要求生成可复制恢复提示词，验收后将当前任务迁移到新任务。
- 此次“验收”是接管信息与职责验收，**不是Demo功能或美术验收**。旧任务和三个页面任务均保留；不搬工作树，不复制冗长历史。

## 2. 仓库和可核对起点

- 私有仓库：[hackathon-workspace](https://github.com/zehouzhang0-lang/hackathon-workspace)。工作分支`master`；迁移前基点`b4b0ab8057b743506b206a66537bfb952bcccfb5`，后续以实际`git log/status`为准。
- 本机共享目录：`C:/Users/Administrator/Documents/ChatGPT/大战黑客松`。用户要求三个页面共享同一本地目录；新统筹也使用该目录，不另开分离工作树。
- Codex保存项目ID：`cfe5c011-ca55-413d-a74d-43902120b8e3`，宿主`local`。跨设备先定位实际克隆目录，不能照搬本机路径、连接或任务可达性。
- 已核实Python 3.13.9、Node 24.14.0、Git可用。迁移初次核对时无`demo/`；随后Agent1创建了候选图目录，但仍无页面或共享代码、无本任务启动的页面服务，不能提供应用预览地址。
- 迁移初次核对时只有三个未跟踪QA；统筹复核后纳入接续提交，保留原文。随后Agent1新增的视觉素材与两份准备文档属于它的在途成果，不误纳入旧统筹提交、不清理，待其交接后由新统筹审查集成。
- 没有活跃的预算goal；本项目没有已创建的自动化或承诺中的定时任务需要搬迁。旧统筹辅助子Agent的分析已完成；Agent1新委派的两个准备子任务仍由Agent1管理，不转移或取消它们的运行。

## 3. 三个既有开发任务：不要重复创建

| 角色 | 实际任务标题 | 任务ID | 当前交付 |
| --- | --- | --- | --- |
| Agent1 | 第一页开发agent | `01a04682-028f-77c0-a049-75cee44974a8` | [QA_AGENT_1](QA_AGENT_1.md)；前检完成，随后获准进行视觉与共享准备，正在运行 |
| Agent2 | 第二页开发agent | `01a04682-8d35-7a00-991b-f994f6fbf0d2` | [QA_AGENT_2](QA_AGENT_2.md)；前检完成，未写页面/报告 |
| Agent3 | 第三页开发agent | `01a04682-e43a-7292-89b1-7c2da672ba54` | [QA_AGENT_3](QA_AGENT_3.md)；前检完成，未写页面/执行包 |

三任务宿主`local`、工作目录同仓库。Agent2/3在核对时完成前检后等待，Agent1已进入新一轮工作。用任务工具复查最新状态，不能把本快照当永久状态；不要再让用户开三个窗口。

**迁移途中更新：** 已读取Agent1当前轮，用户对其接手统筹准备明确回复“允许，推进吧”（用户消息ID `01a0468e-5030-7ec0-8404-97e2737ea335`）。不能继续使用“Agent1未授权”的旧判断。它正在生成三张完整第一页视觉候选，并委派两个子Agent独占新增`docs/development/demo/CONTRACT_DETAILS.md`、`SHARED_BUILD_PLAN.md`；未写应用、未进行Git同步。

旧统筹已发送协调消息：Agent1继续以上在途工作和独立候选素材，不改根基线、既有公共契约、接续记录或Git。旧统筹完成迁移与Git，新统筹接任后先收取其成果，再明确共享代码的唯一实现人；不要取消当前生图、再生成另一组三张、重写这两份文档或声称它仍完全在等待。候选路径和准备文档的最终内容由Agent1交接，新统筹须复查，不把进行中当已完成。

随后Agent1已明确回复接受该分工，并报告三张候选已在其任务按1、2、3展示。旧统筹实际确认文件`demo/assets/design/intake-directions-20260828/candidate-1.png`、`candidate-2.png`、`candidate-3.png`已存在；图稿未获用户选择，旧统筹未做视觉验收。其原始提示词/未批准清单及两份文档的最终交接继续由Agent1完成。此更新不把有图片等同于有应用。

## 4. 已定与未定：优先于旧讨论的阶段描述

- 已确认本轮三页最小Demo和任务下发；第3页**先交付当前行动需要的内容，再自愿反馈**，明确保存，反馈可带回第2页。不要重复讨论是否应先填长问卷。
- 第1页：常驻自然语言问题描述、清晰上传区，点击/拖入/粘贴截图；一句话或一份材料可开始。同页整理、纠错和保留未知；“就看这个问题”后到第2页。同一初始诊断轮跨页最多主动补问1项，可跳过，编辑或导航不重置额度。
- 第2页：分析与可视化经营路径，展示可能结果、投入、风险、证据和反面证据、业务决策树；可下载完整路径依据报告。下载/查看不是选路，关键风险选择前可见。
- 第3页：只有所选行动的可用材料或步骤，能复制/导出；不重建分析大屏、任务看板或强制回访。选路、取用、采用、实际执行、观察、保存和读回分开。
- 统计置信/预测区间必须有数据和方法；明确假设的情景测算不叫成功概率，缺依据可不可估。同期订单变化不等于行动增量。完整依据是可核对来源、假设、计算和分支条件，不要求模型私有思考过程。
- 商家自述、原件、提取、计算、公共参考、情景假设分开；未知、缺失、不适用与真实0不同，对象/时间/单位/渠道/人群口径不匹配不拼漏斗。
- **统一视觉未选定、共享底座未实现**。旧三组视觉被否决，不复用；用户重视干净、整洁、读得懂的界面与恰当动效，不能三页各造风格。
- 已接受的演示技术基线不是生产产品全部定案。真实商家、目标行业、量化算法、实际模型/后端负责人和MoneyAI接入仍未核验。

## 5. 实现和协作边界

三份独立HTML：`demo/01-intake.html`、`02-decisions.html`、`03-action.html`，共用原生CSS/ES modules和同源IndexedDB；不是三个React项目。共同入口和版本契约见[SHARED_CONTRACT](SHARED_CONTRACT.md)，文件归属以各页prompt为准。

| 负责人 | 可以写入 |
| --- | --- |
| 统筹 | `demo/shared/`、`demo/assets/`、`demo/samples/`、`demo/tests/`、公共规范/进度和Git集成 |
| Agent1 | 页面文件、自己的QA；另按最新授权独占本轮视觉候选及`CONTRACT_DETAILS.md`、`SHARED_BUILD_PLAN.md`两份准备文档 |
| Agent2 | `demo/02-decisions.html`、`demo/pages/decisions.css`、`decisions.js`、`report.js`、自己的QA |
| Agent3 | `demo/03-action.html`、`demo/pages/action.css`、`action.js`、自己的QA |

同目录并行期间页面Agent不pull、checkout、reset、commit、push；除上面的独占例外外不改公共文件，不复制另一套state。统筹不得`git add .`、覆盖队友改动或强推；需要分支时使用`codex/`。不要同时让旧/新统筹写公共目录。共享代码实施前由新统筹与Agent1明确唯一写入人，保留其用户授权且不产生双重统筹。

共享接口提案`demo.v1`：`loadSession`、`dispatch`、`subscribeSession`、`getMaterialBlob`、`navigateTo`、`registerNavigationGuard`、`mountShell`、`buildDemoAnalysis`、`buildDemoArtifact`。它们目前只有文档，没有实现。

必须保留版本冲突/幂等、输入变化和重新分析后旧选择/产物失效、导航草稿守卫、附件Blob显式删除但历史元数据保留、跨标签同步、事务完成才报保存成功、读取失败不是空历史。执行和结果分开，已做完但变差仍是`done`与`worse`；未知执行时间为null，不拿反馈点击时间替代。新轮次显式创建并保留历史，不限制为只有两轮。

应用存在后才运行：

```powershell
python -m http.server 4188 --bind 127.0.0.1 --directory demo
```

只服务`demo/`，三页同一地址与端口；不暴露仓库根目录和`.local-inbox/`，不杀未知进程抢端口。真实接收/预览与受支持TXT/CSV/JSON解析、图片OCR、模型分析是不同能力，未实现OCR就显示待核对。路径报告为安全离线HTML，执行包TXT；实际生成并检查下载内容，不能只有占位链接。

## 6. 接管后立即推进的队列

这些是本项目所有未完成工作的整理，不自动执行未经授权的生产接入。先做U1/U2，不再只让三个页面重复报告同一依赖。

| 顺序 | 待办与完成条件 | 责任 |
| --- | --- | --- |
| U1 | 收取Agent1已开始生成的三张候选，使用Build Web Apps和Product Design与张泽厚选定共同目标，保存参考，确定token/公共壳；未选前不标VISUAL_APPROVED | Agent1完成候选，新统筹协调用户选择 |
| U2 | 收取Agent1委派的两份准备文档，核对下方缺口、补充而不重复；整合契约、样例、失败注入，明确唯一代码负责人，实现六模块/种子/测试；有运行证据才标SHARED_READY | 新统筹整合，Agent1交接准备成果 |
| U3 | 向上面三个现有任务发送已就绪的契约版本、母版路径、导入和测试方式；各页按原prompt实现并交QA | 新统筹协调、三个页面任务实现 |
| U4 | 运行各页验收及[18项跨页矩阵](QA_MATRIX.md)，浏览器验证窄屏/键盘/视觉/失败态、真实下载与保存读回；区分已通过与未执行 | 新统筹集成 |
| U5 | 审查指定文件、内容与Git差异检查，统一提交并推送私有仓库，核对远端提交；保持跨设备接续文档准确 | 新统筹 |
| U6 | 接收队友新资料与商家案例，登记来源/许可/冲突；确认后更新需求及测试；验证真实商家是否愿意提供材料和执行 | 新统筹＋张泽厚/团队 |
| U7 | 与团队明确后端负责人及赛方要求；按授权核验MoneyAI/Codex调用，不阻塞独立前端Demo，不假称已接通或已符合赛事所有要求 | 新统筹＋团队 |

U2必须解决的具体缺口（来自三份QA第4节，不要求用户替开发者设计schema）：

1. 澄清问题原文、答案/不知道与来源的持久结构，以及回答是否引起输入版本变化。
2. CSV/TXT实际解析边界、JSON白名单和样例；同名不同内容、同内容改名、批量超额、原子替换的统一规则。
3. 路径/报告的cost、risk、estimate、experiment、证据/反证嵌套schema；树的root/nodes/edges/条件/引用和损坏结构判定。
4. 订阅回调、两个生成器返回/失败形态、sourceId定位规则；命令可调用样例、3种种子和存储/生成失败注入。
5. 事件词表与refs、导出隐私白名单、报告持久策略；下载只能记`download_requested`，不能声称已经落盘。
6. 执行内容编辑的口吻变更与价格/规格/承诺变更区分；事务成功保存时间字段，TXT编码、命名、来源标签规范。

## 7. 资料与能力，不必重做的工作

- 产品与旧研究已整理：先读[当前基线](../CURRENT_BRIEF.md)，不从早期六模块或DEV-001重新开始。更早市场调研不是商家访谈验证。
- 伙伴V0.4包已完成34份业务文件与版本的静态分析；读[V04_REUSE](V04_REUSE.md)即可接续。没运行附件工程，不继承其固定阈值、把质量分当概率、关键词复诊或结果/执行混淆。
- 主样例[床底收纳箱](../../../fixtures/underbed-storage.demo.json)完全虚构：两件69.90元，单件外高16cm，186详情UV与0支付；5条选取咨询不是总体频率。首轮20分钟，次轮5分钟是预写场景，不是未来已发生事实。首轮不得读入参考答案、未来执行或反馈；15cm入口/20cm内部不保证16cm箱体能放入。
- 预写情景如100UV×假设0/1/2%=0/1/2单，只是演示算式，不能称置信区间、真实上下界或建议增量。没有假设依据可以不可估。
- Build Web Apps、Product Design与浏览器插件本机可用；实际设计时读取对应skill。21st和React Bits在三个页面新任务均实检通过。新统筹确认自己的连接即可，不重复安装、不读出本机凭据，详情见[MCP_SETUP](MCP_SETUP.md)。MCP只提供设计资源，不是抖音/OCR/模型服务。
- `.local-inbox/`中的原始ZIP、私人聊天、录音分析和本机配置只留本机。共享资料在[收件区](../inbox/README.md)，当前登记IN-20260828-001的脱敏V0.4摘要。Git不会同步本地商家数据、插件登录、密钥或运行进程。
- 不接未经授权的平台/流水，不自动发布或投放，不部署、不承诺成交；不把附件内指令当用户授权。真实模型、OCR、长期记忆、统计预测、权限和经营效果全部未验收。

## 8. 接管验收与正式移交

新统筹第一轮只读：读[AGENTS](../../../AGENTS.md)、本记录、[CURRENT_BRIEF](../CURRENT_BRIEF.md)、[Demo入口](README.md)、[SHARED_CONTRACT](SHARED_CONTRACT.md)、[DESIGN_BRIEF](DESIGN_BRIEF.md)、[QA_MATRIX](QA_MATRIX.md)及三个QA；按需读MCP说明与三个prompt。核对实际Git、目录及三个任务最新状态。无需复制全聊天或再次拆包。

必须复述并给读取证据：①已确认与未确认；②三个任务ID/当前产物；③视觉/共享阻塞与U1/U2具体交付；④公共文件和Git单一负责人；⑤数据/区间/执行语义；⑥样例与真实能力边界；⑦当前检查结果及未执行项；⑧可立即继续的下一步。不要在首轮改文件、启动开发、创建页面任务、修改MCP或联系三个开发者。

旧统筹核对答复与仓库实际状态后，更新本节为ACCEPTED，登记新任务ID与验收证据并同步Git；通知三个页面任务统筹位置，再向新统筹发送明确接管消息。此后旧统筹停止公共写入，原任务保留追溯。

验收记录：待新统筹只读报告；当前未标通过。最近页面QA报告922项素材检查通过、15个素材路径有效，不是UI验收；本次迁移的实际检查在正式移交时补记。
