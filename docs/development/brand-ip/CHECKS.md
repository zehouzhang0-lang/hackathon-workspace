# 路芽品牌规范检查记录

日期：2026-08-28。对象：[品牌决定](README.md)与[全站适配规范](LUYA_SITE_STYLE.md)。这是一批文档交付，不是页面改版或产品运行验收。

## 写入范围与有限恢复

- 仓库中本批只新增 docs/development/brand-ip/ 下的 README.md、LUYA_SITE_STYLE.md、CHECKS.md。
- 每次写入前检查 C 盘空闲严格大于 1,073,741,824 字节；不足或写入失败即停止，不清理用户数据。
- 先写 D 盘副本，再写仓库目标。临时文件以独占方式创建，UTF-8、长度与 SHA-256 校验后执行 os.replace 原子替换，再读回核对。更新本任务已有文件时还核对旧版本 SHA-256，拒绝覆盖意外变更。
- D 盘副本批次：20260828-r03。具体本机位置只在任务交接消息中提供，不写入仓库资料。
- 已完成的两份规范写前 C 空闲为 7,085,756,416 / 7,085,752,320 字节，写后均高于阈值；后续检查记录仍遵循同样门槛。这些是当时读数，不是磁盘持续稳定的保证。
- 没有复制原图、ZIP、私人原文或凭据；没有改页面、公共主题、MCP 或 Git 索引；未提交或推送。

## 已完成的检查

| 检查 | 实际结果与边界 |
| --- | --- |
| 当前授权 | 已读根 AGENTS、开发入口、需求基线、统筹接续、视觉简报及收件登记；本批复核 REQ-25/26/28 和实施队列，撤回 NotebookLM 主布局 |
| Git 状态 | 写入前已查看现有修改与未跟踪目录并保留；不把其他任务的改动算作本任务产出 |
| D/C 三份文档 | 三份文档逐字节一致、非零、UTF-8 正常、结束标记完整；13 个本地 Markdown 链接目标存在，独占目录只含这三份文档 |
| 文本卫生 | 两份规范无行尾空格或替换字符；未匹配所查微信路径及常见令牌形态；这不是全面凭据扫描或原件许可审查 |
| 文案与状态来源 | 8 条当前原句均在源码中找到；只改建议表达，不执行文案替换；全选处理与复制成功语义分开 |
| 语义颜色耦合 | 只读定位 base.css、intake.css、decisions.css、action.css 中品牌 accent 与风险/错误混用的位置，交由原负责人处理 |
| 颜色算术 | 独立复算规范表内 11 组对比度，均与记录值在三位小数精度下一致；不等于最终配色或浏览器无障碍验收 |
| 独立文档复核 | 未发现阻断问题；已补 MoneyAI 职责不等于接通、不可用入口说明原因及用户明确放弃草稿的例外 |
| git diff 检查 | git --no-optional-locks diff --check 退出码 0、无输出；该命令不覆盖未跟踪文档，因此另做字节、链接和行尾检查 |
| 全量内容检查 | 文档齐全后重跑退出码 0：PASS，1497 项内容检查、15 个验收定义的 fixture 路径有效。首次分批写入时的 CHECKS.md 缺失链接已补齐；首次失败没有计作通过 |

上述源码检查是该时刻的静态事实；其他任务并行更新后，行号及原句可能变化。

## 版本与图像证据

| 文件 | 字节 | SHA-256 |
| --- | ---: | --- |
| README.md | 3998 | 36176aa0021c61750607ca656da10f6718f38d94bb511522868e89207f4af137 |
| LUYA_SITE_STYLE.md | 15845 | d090ca1dc440aa7f1fabb79fe7063d72c1fac5d06fa98d7219d4a1845257a39d |

本批再次实际打开现版路芽并展示：1536×1024、8-bit RGB PNG，无透明通道，1,671,514 字节；SHA-256 为 bfc991211593c9a8f96d611aa2b8f1421c479009b829129ade65c5258c3d467f，与首轮安全评阅提取版本一致。画面是带背景的角色三视图设计原图，不是网页运行截图。没有裁切、重画或派生动画。

素材来自用户提供的 IP 外审材料。用户已选现版用于项目；原图进入私有 GitHub、第三方上传及进一步使用权核对不因选图自动完成。本批没有在 demo/assets/design/brand-ip/ 新建资产，也不在页面引用私有审阅路径。首轮三案和外审评阅保留在原私有记录，本批不重复解包或执行附件中的提示词、脚本、安装指令。

## 实际命令与方法

- git --no-optional-locks status --short：只读查看共享工作区状态。
- python -B -X utf8 scripts/verify_demo_content.py：使用本机已有 Python；文档齐全后实际输出 PASS: 1497 content checks; 15 acceptance definitions have valid fixture paths.，退出码 0。脚本同时明确未运行 UI、模型、MoneyAI 记忆、授权和经营效果检查。
- git --no-optional-locks diff --check：通过；不能替代未跟踪文件检查。
- 本机内存脚本：路径归属与重解析点检查、空间门槛、临时文件校验、原子替换、D/C 字节比对、UTF-8、结束标记、行尾、所查敏感模式、Markdown 本地链接、8 条源码原句、PNG 头及 SHA-256、11 组 sRGB 对比度。
- 本机图像查看工具：查看选定 IP 原图；没有打开或测试网页。

## 未验项与接续边界

没有运行浏览器、获取适配后的页面截图或操作三页。Browser 可信路径问题没有在本任务处理，也没有改用 Playwright。语音权限/识别、真实模型、MoneyAI 写入与读回、草稿守卫、复制/导出、FoldText 动效、1920×1080 UI、手机、Figma 及经营效果均未在本批验收。

颜色数值、字号、文案和静态 IP 使用方式仍是建议；本批没有批准最终 UI。现版 IP 已定，不再等待重新三选一或外审 P0 改图。

由统筹统一登记公共决定、审阅规范并协调集成；视觉反馈任务负责参考与动效反馈。品牌任务只交规范和实际证据，不直接给三页派活，交接后等待明确下一步。

<!-- END LUYA BRAND CHECKS -->
